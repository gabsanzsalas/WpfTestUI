<UserControl x:Class="WpfTestUI.FillControl"
             x:Name="FillControlContext"
             DataContext="{Binding ElementName=FillControlContext}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfTestUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <local:UIStatus  x:Name="UIStatusR" UIStatusLabel="{Binding RectangleStatus, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="800"/>
        <Rectangle x:Name="rectangle" Stroke="Black" Fill="{Binding Path= RectangleFill}" Grid.ColumnSpan="2"/>
    </Grid>
</UserControl>
