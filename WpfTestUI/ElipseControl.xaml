<UserControl x:Class="WpfTestUI.ElipseControl"
             x:Name="ElipseControlContext"
             DataContext="{Binding ElementName=ElipseControlContext}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfTestUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <local:UIStatus  x:Name="UIStatus" UIStatusLabel="{Binding ElipseStatus, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="800"/>
        <Ellipse HorizontalAlignment="Center" x:Name="Ellipse"  Height="122" Stroke="Black" Fill="{Binding Path= ElipseFill}" VerticalAlignment="Top" Width="294"/>
    </Grid>
</UserControl>
