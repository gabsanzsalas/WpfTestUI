<UserControl x:Class="WpfTestUI.BackgroundControl"
             x:Name="backgroundContext"
             DataContext="{Binding ElementName=backgroundContext}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfTestUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid  Background= "{Binding Path= BackgroundFill}">
        <local:UIStatus  x:Name="UIStatusB" UIStatusLabel="{Binding BackgroundStatus, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Center" VerticalAlignment="Center" Width="800"/>
    </Grid>
</UserControl>
